#include <stdio.h>

/**
 * main - print the name of program
 * @argc: is a count of the argument
 * @argv: is an array of pointers
 * Return: 0
 */

int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
